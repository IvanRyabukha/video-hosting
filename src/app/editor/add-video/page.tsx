import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add Video",
};

export default function AddVideoPage() {
  return (
    <div>
      Add Video page
    </div>
  );
}
